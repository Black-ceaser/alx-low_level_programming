#include <stdio.h>
/**
 *main - A program that prints a string of text using the prinrf function
 *Return: (0) Success
 */
int main(void)
{
	printf("with proper grammar, but rhe outcome is a piece of art,"\n);
	return (0);
}
