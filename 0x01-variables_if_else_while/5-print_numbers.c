#include <stdio.h>

/**
 * main - Printing values in base ten
 * Return: (0)Success
 */
int main(void)
{
	int number;

	for (number = 0; number <= 9; number++)
	{
		printf("%d\n", number);
	}
	return (0);
}
